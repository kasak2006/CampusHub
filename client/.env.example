# Base URL for the API. Leave blank in dev to use Vite's proxy (relative /api).
# In production, set to the deployed backend origin, e.g. https://api.example.com
VITE_API_BASE_URL=

# Socket.io server URL. Blank in dev = same origin (proxied by Vite).
VITE_SOCKET_URL=
